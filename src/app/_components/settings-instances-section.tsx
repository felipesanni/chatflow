"use client";

import * as React from "react";

type SettingsInstancesSectionProps = {
  children: React.ReactNode;
};

export function SettingsInstancesSection({ children }: SettingsInstancesSectionProps) {
  return <>{children}</>;
}
