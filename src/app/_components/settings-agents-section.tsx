"use client";

import * as React from "react";

type SettingsAgentsSectionProps = {
  children: React.ReactNode;
};

export function SettingsAgentsSection({ children }: SettingsAgentsSectionProps) {
  return <>{children}</>;
}
