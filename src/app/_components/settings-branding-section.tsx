"use client";

import * as React from "react";

type SettingsBrandingSectionProps = {
  children: React.ReactNode;
};

export function SettingsBrandingSection({ children }: SettingsBrandingSectionProps) {
  return <>{children}</>;
}
